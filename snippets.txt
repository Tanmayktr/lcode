#include <iostream> using namespace std; int main() { cout << "Hello World"; return 0; }
int a = 5, b = 10; swap(a, b); cout << "Swapped: " << a << " " << b;
for(int i=0; i<10; i++) { if(i%2==0) cout << i << " is even" << endl; }
int arr[] = {1, 2, 3, 4, 5}; int n = sizeof(arr)/sizeof(arr[0]); cout << "Size: " << n;
auto sum = [](int x, int y) { return x + y; }; cout << "Sum: " << sum(10, 20);
int n = 7; if(n & 1) cout << "Odd number via bitwise check";
vector<int> v = {10, 20, 30}; v.push_back(40); cout << "Vector size: " << v.size();
struct Node { int data; Node* next; }; Node* head = new Node{10, NULL};
int x = 10, y = 20; int maxVal = (x > y) ? x : y; cout << "Max is: " << maxVal;
while(n > 0) { cout << n % 10; n /= 10; } // Reverse digits logic
